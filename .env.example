# Server-side proxy configuration. Never prefix the token with VITE_.
JOBRADAR_API_ORIGIN=http://localhost:8000
JOBRADAR_API_TOKEN=

# Enable only when building this checkout for OpenAI Sites.
OPENAI_SITES=false
