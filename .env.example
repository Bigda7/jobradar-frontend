# Local development: use the Vite proxy configured in vite.config.ts.
VITE_API_BASE_URL=/api

# Production (Vercel): set this value in Project Settings -> Environment Variables.
# VITE_API_BASE_URL=https://api.yourdomain.com
